/*
console.log('it works');

const name = 'cin';
const last = 'liu';

const sentence = "she's so cool";

const age = 100;

const person = {
    first: 'wes',
    last: 'bos',
    age: 100
};

let dog; // returns undefined if not set
dog = 'snickers';

const cher = {
    first: 'cher'
};

// cher.last returns undefined

const teller = {
    first: 'Raymond',
    last: 'Teller'
};

teller.first = 'Teller';
teller.last = null;

// teller.last returns null

*/

const isDrawing = false;
let age = 18;
const ofAge = age > 19;

console.log(ofAge);

age = 100;

const age2 = 100;

// === checks value and type are the same
